external procedure PrintAgreeInvRn(record RcVc);

global
procedure AgreementInfoRn(record RcVc RepSpec)
BEGIN
  StartReportJob(USetStr(11401));
  EndHeader;
  PrintAgreeInvRn(RepSpec);
  EndJob;
  RETURN;
END;
