external updating procedure TRAT2DeprCalculation(record RcVc,Integer);

global
updating procedure AT2GenTRRn(var record RcVc RepSpec)
BEGIN        
  StartReportJob(USetStr(5310));
  EndHeader;
  RepSpec.ArtMode = 1;  
  TRAT2DeprCalculation(RepSpec,2);  
  EndJob;
  RETURN;
END;
