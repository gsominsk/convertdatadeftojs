begin
  string 255 hobname;
  
  SetLangMode(LangEnglish,"ENG",0);
  
  SetBuildProductCode("Books");
  SetBuildProgramType(typStandardERP); // typBooks??
  
  hobname = "hob/startupbks.hob"; //common startup hob (after DB is open) for Books products (all languages)
  Delete_File(hobname);
  Compile_File("hal/Startup/BooksStartup.hal",hobname);

  return;
end;
