global
function Boolean RepGenDClassActiveEditField(Integer wn,string fieldname,Integer fn,Integer wnst,Integer rownr,Integer changed)
BEGIN
  Boolean res;
  
  res = true;
/* should be possible to change
  if (wnst==2) then begin//Rs_update
    switch (fieldname) begin
      case "Code": res = false;
    end;
  end;
*/
  RepGenDClassActiveEditField = res;
  RETURN;
END;
