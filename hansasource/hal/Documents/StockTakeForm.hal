global
procedure StockTakeForm(record RcVc RepSpec)
BEGIN
  record StockTakeVc STr;
  Boolean tmpf;
  
  if (RepSpec.long1>0) then begin
    STr.SerNr = RepSpec.long1;
    ReadFirstMain(STr,1,true);
  end;
  tmpf = PrintDocument(STr,"StockTakeForm",false);
  RETURN;
END;

